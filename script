pipeline {
    agent any
    stages{
        stage("Parallel"){
            steps{
                parallel(
                    a:{
                        bat "mvn clean"
                    },
                    b:{
                        bat "mvn test"
                    },
                    c:{
                        bat "mvn deploy"
                    }
                )
            }
        }
        
    }
}
